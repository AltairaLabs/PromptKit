# yaml-language-server: $schema=https://promptkit.altairalabs.ai/schemas/latest/provider.json
#
# Google Gemini 2.0 Flash Provider
# Native audio streaming support for bidirectional real-time conversations.
#
apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: gemini-2-flash

spec:
  id: gemini-2-flash
  type: gemini
  model: gemini-2.0-flash-exp

  # Provider defaults
  defaults:
    temperature: 0.7
    top_p: 1.0
    max_tokens: 1000

  # Gemini-specific configuration for audio streaming
  additional_config:
    # Enable audio modality for duplex mode
    audio_enabled: true
    # Response modalities (text and audio)
    response_modalities:
      - text
      - audio
