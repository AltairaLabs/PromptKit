apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Provider
metadata:
  name: mock-dataviz
spec:
  id: mock-dataviz
  type: mock
  model: mock-dataviz-model
  additional_config:
    mock_config: mock-responses.yaml
  defaults:
    temperature: 0.7
    max_tokens: 1000
    top_p: 1.0
