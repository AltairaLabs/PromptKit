apiVersion: promptkit.altairalabs.ai/v1alpha1
kind: Arena
metadata:
  name: llm-judge-demo
spec:
  defaults:
    concurrency: 1
    verbose: true

  prompt_configs:
    - id: judge-simple
      file: prompts/judge-simple.prompt.yaml
    - id: judge-evidence
      file: prompts/judge-evidence.prompt.yaml

  # Providers used by the main assistant (mock)
  providers:
    - file: providers/mock-assistant.yaml
      group: default
    - file: providers/mock-judge.yaml
      group: judge

  # Judges map names to provider IDs and optional model overrides
  judges:
    - name: mock-judge
      provider: mock-judge
      model: judge-model
  judge_defaults:
    prompt: judge-simple

  scenarios:
    - file: scenarios/helpfulness.yaml
