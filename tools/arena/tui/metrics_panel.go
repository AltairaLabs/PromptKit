package tui

const (
	metricsPanelWidth        = 40
	metricsPaddingVertical   = 1
	metricsPaddingHorizontal = 2
)

// renderMetrics was removed as dead code - never called in production.
// Metrics are displayed via the summary pane instead.
// The constants above are kept for potential future use.
