// Command multitrack-ingestion is a generic demo of sdk.MultiTrackIngestion:
// two speakers' audio arrive on separate tracks, are transcribed independently,
// and the merged speaker-labeled transcript drives a per-turn assistant over a
// WithIngestion duplex. Run keyless by default; pass --live for real Claude.
//
// The real entry point is filled in once the conversation wiring exists; this
// stub keeps the package building as a binary during scaffolding.
package main

func main() {}
