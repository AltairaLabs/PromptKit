---
import { getCollection } from 'astro:content';
import DocsLayout from '../../layouts/DocsLayout.astro';
import Sidebar from '../../components/Sidebar.astro';

const arenaIndex = await getCollection('arena', ({ id }) => id === 'index.md' || id === 'index');
const indexEntry = arenaIndex[0];

let title = 'PromptArena';
let description = 'Test and evaluate prompts across multiple LLM providers';
let Content;

if (indexEntry) {
  const rendered = await indexEntry.render();
  Content = rendered.Content;
  title = indexEntry.data.title || title;
  description = indexEntry.data.description || description;
}
---

<DocsLayout 
  title={title}
  description={description}
  product="arena"
>
  <Sidebar slot="sidebar" product="arena" currentPath={Astro.url.pathname} />
  
  {Content ? <Content /> : (
    <div>
      <h1>🏟️ {title}</h1>
      <p class="lead">{description}</p>
      <p>Welcome to the PromptArena documentation.</p>
      <p>Select a topic from the sidebar to get started.</p>
    </div>
  )}
</DocsLayout>

<style>
  .lead {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    color: #57606a;
  }

  @media (prefers-color-scheme: dark) {
    .lead {
      color: #8b949e;
    }
  }
</style>
